import styled from 'styled-components'

export const ProductCard = ({ img, title, details, price }) => {
  return <CardContainer>index</CardContainer>
}

const CardContainer = styled.article`
  height: 53rem;
  width: 33rem;
  box-shadow: 4px 4px 4px rgba(0, 0, 0, 0.1);
  margin: 3rem auto;
`
